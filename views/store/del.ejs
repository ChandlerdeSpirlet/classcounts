<!--Including Header partial-->
<%- include ../layouts/header2.ejs %>

<% if (messages.error) { %>
    <p style="color:red"><%- messages.error %></p>
<% } %>
<% if (messages.success) { %>
    <p style="color:green"><%- messages.success %></p>
<% } %>
<!--Create a form to take input from user-->
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<style> 
    body {
        font-family: "Trebuchet MS", Arial, Helvetica, sans-serif;
    }
    input[type=text] {
        width: 130px;
        box-sizing: border-box;
        border: 2px solid #ccc;
        border-radius: 4px;
        font-size: 16px;
        background-color: white;
        background-position: 10px 10px; 
        background-repeat: no-repeat;
        padding: 12px 20px 12px 40px;
        -webkit-transition: width 0.4s ease-in-out;
        transition: width 0.4s ease-in-out;
    }
    
    input[type=text]:focus {
        width: 100%;
    }
    input[type=number] {
        width: 130px;
        box-sizing: border-box;
        border: 2px solid #ccc;
        border-radius: 4px;
        font-size: 16px;
        background-color: white;
        background-position: 10px 10px; 
        background-repeat: no-repeat;
        padding: 12px 20px 12px 40px;
        -webkit-transition: width 0.4s ease-in-out;
        transition: width 0.4s ease-in-out;
    }
    
    input[type=number]:focus {
        width: 100%;
    }
    [type=submit] {
    background-color: blue; /* Green */
    border: none;
    color: white;
    padding: 16px 32px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 4px 2px;
    -webkit-transition-duration: 0.4s; /* Safari */
    transition-duration: 0.4s;
    cursor: pointer;
    }

    [type=submit] {
        background-color: white; 
        color: black; 
        border: 2px solid blue;
    }

    [type=submit]:hover {
        background-color: blue;
        color: white;
    }
    input:invalid+span:after {
        content: '✖';
        color: black;
        padding-left: 10px;
        position: fixed;
        padding-top: 15px;
    }
</style>
<form action="/store/del" method="post" name="form1">
    <table width="25%" border="0">
        <tr>
            Barcode is required for removal, in order to prevent mistakes.
        </tr>
        <tr>
            <td>Barcode</td>
            <td><input type="number" pattern="\b[0-9]{5}\b" min="0" max="99999" name="barcode" value="<%= barcode %>"/>
                <span class="validity"></span></td></td>
        </tr>
        <tr>
            <td>Name</td>
            <td><input type="text" name="bbname" pattern="[A-z]{1,20}+" min="1" maxlength="20" id="name" value="<%= bbname %>"/>
                <span class="validity"></span></td></td>
        </tr>
        
        <tr>
            <td></td>
            <td><input type="submit" onclick="return confirm('Are you sure you ' +
                'want to delete this blackbelt, this will remove them from the table?')"name="Submit" value="Remove"/></td>
        </tr>
    </table>
</form>
</head>
<!--Including Footer partial-->
<%- include ../layouts/footer.ejs %>